@Configuration
@ComponentScan("package")
@ComponentScan(basePackages = {"configcenterweb_v6", "else"})
public class CDPlayerConfig {
     /*
        JavaConfig配置文件
        配置文件自动装配、遍历所有的Component，并自动创建对应的Bean
     */
}
